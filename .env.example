# Greenhouse Harvest API Configuration
# Copy this file to .env and fill in your actual values
#
# Use EITHER api_key (V1 Basic Auth) OR client_key + client_secret (V3 OAuth)

# Option 1: V1 Basic Auth - Use your Harvest API key
# TAP_GREENHOUSE_API_KEY=your-harvest-api-key

# Option 2: V3 OAuth - Use client credentials
TAP_GREENHOUSE_CLIENT_KEY=your-client-key
TAP_GREENHOUSE_CLIENT_SECRET=your-client-secret

# Optional: Start date for incremental sync (ISO 8601 format)
# TAP_GREENHOUSE_START_DATE=2024-01-01T00:00:00Z

# Optional: API URL (defaults to https://harvest.greenhouse.io/v1)
# TAP_GREENHOUSE_API_URL=https://harvest.greenhouse.io/v1
