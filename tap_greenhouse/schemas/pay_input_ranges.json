{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "integer"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "pay_input_id": {
      "type": "integer",
      "description": "Id of the `pay_input` this range fills in (e.g. the `Base Salary` pay input on a pay transparency rule). Multiple ranges can share a `pay_input` when a job post has more than one component (e.g. base salary plus bonus)."
    },
    "currency_type": {
      "type": "string",
      "description": "ISO 4217 currency code for `min_cents` and `max_cents` (e.g. `USD`, `EUR`, `GBP`). Defaults to `USD`."
    },
    "min_cents": {
      "type": "integer",
      "description": "Lower bound of the pay range, expressed in the smallest unit of `currency_type` (e.g. cents for `USD`). `0` when the range has not been filled in on the job post."
    },
    "max_cents": {
      "type": "integer",
      "description": "Upper bound of the pay range, expressed in the smallest unit of `currency_type` (e.g. cents for `USD`). `0` when the range has not been filled in on the job post."
    },
    "job_post_id": {
      "type": "integer",
      "description": "Id of the `job_post` this range is attached to. Pay transparency ranges are stored per job post so the same job can publish different ranges on different posts."
    }
  },
  "description": "Schema for the 'pay_input_ranges' stream, linked to the '/v3/pay_input_ranges' endpoint."
}
