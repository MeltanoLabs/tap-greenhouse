{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "integer"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "name": {
      "type": "string",
      "description": "Label of the interviewer tag (surfaced as an Interviewer Group in the Greenhouse UI). Unique per organization and case sensitive; a user can be assigned to multiple tags, and scheduling flows filter the interviewer picker by these labels."
    }
  },
  "description": "Schema for the 'interviewer_tags' stream, linked to the '/v3/interviewer_tags' endpoint."
}
